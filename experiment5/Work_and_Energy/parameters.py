import sympy

F, s, theta = symbols("F s theta")
m, a, v, g, h = symbols("m a v g h")
v_final, v_initial = symbols("v_final v_initial")
h_final, h_initial = symbols("h_final h_initial")

explanation_sample = "Work done is force times displacement times cos of the angle."